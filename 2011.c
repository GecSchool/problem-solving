#include <stdio.h>
#include <string.h>
int main(){
    int dp[5001];
    int number_len;
    char number[5001];
    char *num;
    num = number-1;
    scanf("%s",number);

    // for(int i=0;i<=;i++);
    return 0;
}